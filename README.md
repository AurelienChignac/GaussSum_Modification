# GaussSum_Modification
A modification of the GaussSum application (https://gausssum.sourceforge.net/) for extracting and analysing electronic circular dichroism (ECD) spectra. The modified file electrontrans.py allows to generate two text files: ECD_Spectra.txt containing the ECD spectra data and Transitions.txt containing the transitions
